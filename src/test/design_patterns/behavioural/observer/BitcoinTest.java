package design_patterns.behavioural.observer;

public class BitcoinTest {

    /* All the unit test cases are independent of each other
    // because if one fails it should not be necessary the other fails as well.
    // it will help us understand that which part of our code is failing.
    In order to make out test case independent we create 2 methods

    Setup => Create the dependencies
    TearDown => Destroy the dependencies
*/

}
